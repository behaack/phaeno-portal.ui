export type UserListItem = {
  id: string,
  firstName: string,
  lastName: string,
  isAdmin: boolean
}

export type UserDetails = {
  id: string,
  firstName: string,
  lastName: string,
  isAdmin: boolean
}